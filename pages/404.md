---
layout: page
title: "Page not found"
permalink: /404.html
hide: true
excluded: true
subtitle: "Looks like there has been a mistake. Nothing exists here."
redirect: true
---

<svg width="2rem" height="2rem" viewBox="0 0 40 40" xmlns="http://www.w3.org/2000/svg">
<path d="M17 13.5v6H5v-12h6m3-3h6v6m0-6-9 9" class="icon_svg-stroke" stroke="#999" stroke-width="1.5" fill="none" fill-rule="evenodd" stroke-linecap="round" stroke-linejoin="round"></path>
</svg> Sorry, the requested page wasn't found on the server.

{% if page.redirect %} 
You will be shortly redirected to **{{ site.url }}**. 
{% endif %}

<svg width="300" height="100" viewBox="0 0 300 100">
  <!-- line -->
  <line x1="0" y1="80" x2="200" y2="80" stroke="                       
  <!-- stick man -->
  <g id="stickman" transform="translate(0,0)">
    <circle cx="10" cy="60" r="10" fill="#000" />
    <line x1="10" y1="70" x2="10" y2="80" stroke="                         
    <line x1="10" y1="70" x2="5" y2="75" stroke="#000" stroke-width="2" />
    <line x1="10" y1="70" x2="15" y2="75" stroke="                         
    <line x1="10" y1="60" x2="10" y2="50" stroke="#000" stroke-width="2" />
  </g>
  
  <!-- animate -->
  <animateTransform 
    attributeName="transform" 
    type="translate" 
    from="0 0" 
    to="280 0" 
    dur="8s" 
    repeatCount="indefinite"
    xlink:href="#stickman"
  />
</svg>

<p>
  <svg xmlns="www.w3.org" viewBox="0 0 24 24" width="54" height="54">
  <circle cx="12" cy="12" r="10" fill="gray"/>
  <path d="M8 14s1.5-1 4-1 4 1 4 1" fill="none" stroke="black" stroke-width="2"/>
  <path d="M9 10.5h.01M15 10.5h.01" fill="aqua"/>
</svg>
</p>


  <canvas id="chakraCanvas" width="200" height="200"></canvas>
  <script>
    const canvas = document.getElementById('chakraCanvas');
    const ctx = canvas.getContext('2d');
    let angle = 0;

    function drawChakra() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      ctx.save();
      ctx.translate(canvas.width / 2, canvas.height / 2);
      ctx.rotate(angle * Math.PI / 180);
      ctx.fillStyle = '#FF9933';
      ctx.beginPath();
      ctx.moveTo(0, 0);
      for (let i = 0; i < 24; i++) {
        ctx.lineTo(80 * Math.cos(i * Math.PI / 12), 80 * Math.sin(i * Math.PI / 12));
        ctx.lineTo(0, 0);
      }
      ctx.fill();
      ctx.restore();
      angle = (angle + 5) % 360;
      requestAnimationFrame(drawChakra);
    }

    drawChakra();
  </script>
